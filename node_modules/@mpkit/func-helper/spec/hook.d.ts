import { MkFuncHook, MkFuncHookResult } from "@mpkit/types";
/**使用多种钩子钩住函数，并返回处理后的函数 */
export declare const hookFunc: <T extends Function = Function, S = any>(func: T, hooksInvariant: boolean, hooks: MkFuncHook<S>[], otherState?: any) => MkFuncHookResult;
